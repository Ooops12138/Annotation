# Annotation 项目协作约定

## 项目使命

Annotation（书注）是一个教材驱动的 AI 学习文档生成项目。它把一本或多本教材转换为可追溯、可学习、可审核的在线学习文档。第一阶段只验证“单一学科的一章”闭环，不以实现通用教育平台为目标。

项目的主链路是：

```text
教材导入 → 教材理解 → Learning Blueprint → 内容生成 → 数字化呈现 → 质量检查
```

## 文档优先与信息来源

- `documents/ai_textbook_project_docs/README.md` 是现有设计资料的入口；其余同目录文档是设计依据。
- `docs/requirements.md` 定义当前需求和范围；`docs/architecture.md` 定义结构与边界；`docs/technology-selection.md` 定义已确定的技术路线；`docs/decisions.md` 记录架构决策；`docs/evaluation.md` 定义验收与评估；`tasks/backlog.md` 是实施顺序的唯一任务清单。
- 当新结论改变需求、边界、接口、数据契约或技术方向时，先更新相应文档，再修改实现或任务。
- 教材是内容主来源；外部资料只能用于补充或核查，不能无记录地替换教材主线。
- 所有重要生成内容必须能够回溯到 Learning Blueprint 和教材来源；无法追溯的内容应标记为风险，而不是默认为可靠。

## 工作范围

- 第一阶段优先完成一个章节的端到端 POC：教材导入、蓝图生成、讲解生成、测验生成、基础审核和在线呈现。
- 多教材对齐、交互组件、复杂自动评估、账号体系、协作、移动端和大规模部署不属于第一阶段的默认范围。
- 不因“Agent”这一名称而拆分组件。只有存在独立判断、工具调用、迭代反馈或可验证边界时，才引入独立 Agent 或 loop。
- 除非任务明确要求，否则不要实现业务代码、生产部署、账号系统或通用平台能力。本轮仓库交付以设计和任务文档为主。

## 变更规则

- 先确认变更属于需求、架构、决策、评估还是任务；不要把不同层次的内容混在一个文件中。
- 对尚未验证的内容使用“假设”“暂定”或“待决”，不得把推测写成已实现能力。
- 新增字段、流程或组件时，说明其来源、消费者、失败处理和验收方式。
- 任何生成内容、补充资料、审核结果和重生成结果都应保留版本或运行标识的设计位置，即使具体存储方式尚未决定。
- 不删除原始教材、来源引用、审核痕迹或失败记录来掩盖质量问题。

## 代码与工具约定

- 开始理解已有代码前，若仓库根目录存在 `.codegraph/`，优先使用 CodeGraph（MCP `codegraph_explore` 或 `codegraph explore`）；不存在时再使用 `rg` 等工具。
- 实现阶段优先保持模块边界与文档中的 artifact 契约一致，不要让 Agent 之间通过隐式文本约定传递核心数据。
- 对尚未在 `docs/technology-selection.md` 或 ADR 中确定的模型、向量库、解析器、部署平台和其他依赖，不要在文档或代码中伪装成既定选型。已选定的技术也必须通过适配边界保持可替换。
- 依赖和外部服务的增加必须能说明对 POC 验收的必要性；避免为了“未来可能需要”提前引入基础设施。

## 完成检查

提交任何阶段性结果前，至少检查：

1. 是否仍能从教材追溯到蓝图，再追溯到生成内容和审核结果；
2. 是否覆盖蓝图中的关键知识、前置关系和教学材料；
3. 是否把事实错误、逻辑错误、遗漏和不确定内容区分开；
4. 是否为失败、回退、重生成和人工介入留下明确路径；
5. 是否更新了受影响的设计文档、决策记录和 backlog；
6. 是否超出了第一阶段范围。
